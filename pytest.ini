[pytest]
# 专用 pytest 配置：放根目录，pytest-core 与 pytest-asyncio 都会可靠读取，
# 不受「显式传文件 vs testpaths」调用方式差异影响（pyproject 里 pytest-asyncio 偶发读不到 asyncio_mode）。
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = backend/tests
pythonpath = .
addopts = -q --no-header
